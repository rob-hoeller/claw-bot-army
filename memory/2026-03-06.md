# Memory: 2026-03-06

## Session Summary — Lance (03:03-03:20 UTC)

### Decisions Made

1. **OpenClaw update** — confirmed already on 2026.2.26, removed from pending list ✅
2. **Schellie (SL1) & Competitive Intel (SL2)** — removed from this instance's active registry; currently running on separate OpenClaw instances (DE1 and DE2)
3. **Migration planned** — will consolidate DE1 + DE2 into this instance via node pairing, preserving all context and functionality, then spin down old instances
4. **Codebase audit** — on hold until Claude Code OAuth + tmux team mode is working
5. **New agent: Dannie** — warranty 24/7 agent, to be built AFTER Schellie is migrated here and agent teams are operational so Schellie can contribute to Dannie's design
6. **Agent team philosophy** — all agents should know each other, work together toward Schell's common goal of happiness

### Rob's Task List (for tomorrow)
1. Claude Code OAuth — SSH into main VPS, run `claude`, follow auth URL (~2 min)
2. Node pair DE1 (Schellie) — SSH into DE1, run `openclaw node pair`
3. Node pair DE2 (Competitive Intel) — SSH into DE2, run `openclaw node pair`

### Sequence of Operations
1. Rob does OAuth + node pairing
2. HBx extracts Schellie & Competitive Intel configs/memory from DE1/DE2
3. Rebuild them as sub-agents on this instance
4. Validate functionality
5. Get tmux agent teams working
6. Build Dannie (warranty agent) with Schellie in the loop
7. Kick off full codebase audit with agent teams
8. Spin down DE1/DE2 instances

### HB2 Rebuild — Major Initiative
- **Goal:** Ground-up rebuild of HB1 into new TS/JS agentic stack with Supabase
- **Approach:** Clean room build — HB1 stays live, HB2 built in parallel until feature parity
- **Deadline:** End of March 2026
- **Cursor integration:** HBx + team get direct code + DB access via Cursor
- **Team briefing created:** `/global-knowledge/HB2-MISSION-BRIEF.md`
- Every agent gets the brief on first spawn — full context, full motivation

### Platform State
- 11 active agents, 0 deploying, 2 removed (running elsewhere)
- Platform stable, no issues
- Last feature shipped: PR #88 (Feb 26)

---

## Daily Memory Log — 04:00 UTC
- Cron ran daily-memory-log.sh for all 13 agents → Supabase `memory_logs` updated ✅
- No other sessions active since Lance's 03:03 session
- Waiting on Rob's task list (OAuth, node pairing DE1/DE2) before next major work begins

## Session — 17:39-17:49 UTC (Lance)

### Vercel Relink
- Vercel API PATCH cannot change `gitRepository` — must be done in Vercel dashboard UI
- Steps given to Lance: Settings → Git → Disconnect `schellbrothers/HBx` → Connect `rob-hoeller/claw-bot-army` → Root dir `dashboard`
- Project ID: `prj_s6AqZOrqKxotQ875EJMWzM0vQNGS`, Team: `team_rIwpFdWNu8FSu4tGNphNvOWk`

### Pipeline Automation Engine Feature
- **Feature ID:** `f8e3b0c8-709e-4c68-943e-4da1d8fe5785`
- Created as URGENT, routed through intake → spec
- **Major discovery:** `run-pipeline/route.ts` already has full automation engine (step activities, auto-advance, human gates, QA revision loops, simulated ship). It was BUILT but never worked.
- **4 bugs blocking it:**
  1. `submit/route.ts` missing `action_type` in agent_activity insert (NOT NULL fails silently)
  2. `review-verdict/route.ts` doesn't re-trigger `run-pipeline` after human approval
  3. `features/route.ts` (POST) doesn't set `current_step`/`current_agent` on creation
  4. FeatureBoard approve button does raw PATCH instead of calling review-verdict API
- **Fix is ~30 lines across 6 files** — not a rebuild, just connecting existing wires
- Spec written and stored in `feature_spec` field. Paused at **Human Gate #1 (Spec Review)** — awaiting Lance's approval

### Pipeline Watcher Cron
- Auth integration PR completed (branch `hbx/auth-profile-integration`) — removes dead `UserSelector.tsx` and `useCurrentUser.ts`
- Quality gates passed. Two follow-ups: run profiles migration on prod Supabase, fix hardcoded `chatUsers` in AgentsPage

### CCA Issues
- Claude Code processes still getting SIGTERM'd during long runs (spec analysis attempt killed). Not memory (12GB avail). Likely timeout. Workaround: do analysis manually with read tool instead of spawning CCA.
