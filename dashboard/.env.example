# ===========================================
# HBx Dashboard Environment Variables
# ===========================================
# Copy this to .env.local and fill in your values

# ===========================================
# Supabase Configuration
# ===========================================
# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase service role key (server-side only - KEEP SECRET)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ===========================================
# OpenClaw Gateway Configuration
# ===========================================
# URL of the OpenClaw Gateway
# Default: http://127.0.0.1:18789
OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789

# Gateway auth token
# Get this from your OpenClaw config: gateway.auth.token
# Or set via OPENCLAW_GATEWAY_TOKEN in the gateway environment
OPENCLAW_GATEWAY_TOKEN=your_gateway_token_here

# ===========================================
# Notes
# ===========================================
# 1. Gateway must have chatCompletions enabled:
#    gateway.http.endpoints.chatCompletions.enabled: true
#
# 2. For Vercel deployment, add these as Environment Variables
#    in your Vercel project settings.
#
# 3. Never commit .env.local to version control!

# Anthropic API Key (for direct sub-agent LLM calls — bypasses gateway)
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-20250514
